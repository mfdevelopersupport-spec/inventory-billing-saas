{
  "name": "inventory-billing-saas",
  "version": "1.0.0",
  "description": "Plataforma SaaS Multi-Sucursal de Inventario, Facturación (POS) y Gestión con RBAC",
  "private": true,
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "dev": "npm --prefix server run dev & npm --prefix client run dev",
    "build:server": "npm --prefix server run build",
    "build:client": "npm --prefix client run build",
    "build": "npm run build:server && npm run build:client",
    "db:push": "npm --prefix server run db:push",
    "db:seed": "npm --prefix server run db:seed",
    "start": "npm --prefix server start"
  },
  "keywords": [
    "inventory",
    "billing",
    "saas",
    "pos",
    "multi-branch",
    "rbac",
    "invoicing",
    "pdf"
  ],
  "author": "Development Team",
  "license": "MIT"
}
